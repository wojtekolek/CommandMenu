{
  "name": "cmdmenu",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsup ./index.ts --format esm,cjs --external react",
    "dev": "tsup ./index.ts --format esm,cjs --watch --external react",
    "lint": "eslint *.ts*",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "files": [
    "dist/**"
  ],
  "devDependencies": {
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "eslint": "^7.32.0",
    "react": "18.2.0",
    "tsup": "^6.2.3",
    "typescript": "^4.8.2"
  }
}
